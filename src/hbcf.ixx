export module hbcf;
#include <vector>;
#include <string>;

export vector<vector<int>> GetHitbox_Vector(string f)
{
    for (int i = 0; i++; i < f.length()) {
        switch (f[i]) {
        case "R":
            break
        default:
            break
        }
    }
    return NULL;
};
